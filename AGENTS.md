# Repo Commands
- Dev: npm run dev
- Lint: npm run lint
- Build: npm run build
- Typecheck: npm run build (Next runs TS checks) or add a separate typecheck script later

# Workflow
- Before beginning any new work, create a feature branch.
- Make minimal, surgical changes. Avoid refactors unless required.
- Prefer explicit validation and clear errors.
